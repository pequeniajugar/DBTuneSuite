settings: slide P116
input size recommandation: 100000，10000000
only uniformly distributed data

table:
n = input size
employees(ssnum, name, lat, long, hundreds1,hundreds2);
	ssnum: int	1... n		primary key		number = n
	name: text	Employee1...Employeen		number = n/100
	lat: int	(100, 1100)       			 	number = n/100
	long: int	(100, 1100)    				number = n/100
	hundreds1: int	(100, 1000)    				number = n/100
	hundreds2: int	(100, 1000)    				number = n/100

hundreds1 is sorted asc
ssnum is sorted asc
the rest are shuffled

create schema:
CREATE TABLE employees (
    ssnum INT,
    name VARCHAR(255),    
    lat INT,
    longitude INT, 
    hundreds1 INT,
    hundreds2 INT);

-- Index definitions (logical design; adapt syntax per DBMS):
-- Nonclustered index on hundreds1 (covering some queries)
-- Nonclustered index on hundreds2
-- Additional covering indexes on (ssnum, name, hundreds2) and (lat, ssnum, name)

-- Example (generic SQL-style notation):
-- NONCLUSTERED INDEX c  ON employees(hundreds1);
-- NONCLUSTERED INDEX nc ON employees(hundreds2);
-- INDEX nc3 ON employees(ssnum, name, hundreds2);
-- INDEX nc4 ON employees(lat, ssnum, name);




